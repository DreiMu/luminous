# Luminous Discord Chat Bot
## Usage
Copy .env.example to .env and fill out the empty Fields. Now run cargo run to start the Bot. Note that the Bot uses Luminouses Supreme Model which uses the most Credits.